# Wave 1 Brief — Riley (front-end-architect)

**From:** Marcus (orchestration-manager)
**Date:** 2026-05-21
**Status:** Dispatched

## Project root
`d:\Projects\PDF_Viewer_Editor`

## Must read first (in order)
1. `d:\Projects\PDF_Viewer_Editor\CLAUDE.md` — project rules, locked stack, file ownership
2. `d:\Projects\PDF_Viewer_Editor\docs\project-roadmap.md` — Phase 1–7 roadmap
3. `d:\Projects\PDF_Viewer_Editor\docs\project-plan.md` — full plan including the four locked decisions (Section 5)
4. `d:\Projects\CLAUDE.md` — swarm rules
5. Last 200 lines of `c:\Users\ahudson\.claude\learnings\global.jsonl` (filter for `front-end-architect`)

## Locked decisions you must encode (Section 5 of project-plan.md)

| # | Decision | Encode in |
|---|---|---|
| 1 | **Hybrid Print-to-PDF**: pdf-lib re-emit default; headless Chromium `printToPDF()` fallback. Heuristic + manual override. Both engines designed in NOW. | ARCHITECTURE.md (engine-selector module + heuristic), docs/api-contracts.md (export IPC channel returns `{engine, warnings, outputBytes}`) |
| 2 | **Embed annotations as standard PDF annotation subtypes**. No sidecar. | docs/data-models.md (subtype mapping table), ARCHITECTURE.md (annotation round-trip via pdf-lib + fallback through Chromium engine when needed) |
| 3 | **Redux Toolkit** (NOT Zustand). Command-pattern undo middleware. Multi-slice. | docs/conventions.md (slice conventions), ARCHITECTURE.md (state architecture + undo middleware spec) |
| 4 | **Windows file association: installer checkbox, default ON**. Settings toggle for post-install. No silent re-assertion. | docs/api-contracts.md (`app:setDefaultPdfHandler` IPC channel), docs/ui-spec.md (Settings dialog), data-models.md (`app_settings` key `file_association.pdf.requested`) |

## Deliverables — Wave 1, docs only, NO code

All paths absolute, all writes only to the files listed:

### 1. `d:\Projects\PDF_Viewer_Editor\ARCHITECTURE.md`
Must cover:
- Electron multi-process layout (main / preload / renderer) with a Mermaid diagram of the boundaries
- Security model: contextIsolation: true, nodeIntegration: false, sandbox: true where compatible, no remote module, strict CSP (give the exact CSP string), IPC only via typed preload `contextBridge`
- Library inventory table with: name, version (latest stable as of 2026-05-21), license (verified MIT / Apache-2.0 / BSD only), purpose, which process(es) consume it
- Module boundaries between main / preload / renderer — what each can and cannot do
- Document-model design: in-memory representation of an open PDF (PDFDocument, Page, Annotation, EditOperation as a discriminated union, dirty-state tracking)
- Undo/redo strategy via command pattern, aligning with Redux Toolkit middleware (specify the action `meta.undoable: true` convention and the historySlice shape)
- **Print-to-PDF dual-engine design**: ExportEngineSelector module, the heuristic (encrypted source / xref-repair warnings / CMYK+ICC / unauthorable annotation subtype → Chromium; otherwise pdf-lib), and how the offscreen-BrowserWindow Chromium path works (`pdfedit://` custom protocol, no temp file)
- Extension points for Phase 2–7 features (forms, OCR, scan, Office export, mail-merge, sign, image-import) — call out where each will plug in; do NOT implement

### 2. `d:\Projects\PDF_Viewer_Editor\docs\api-contracts.md`
Every IPC channel for Phase 1 with typed request/response signatures. Channels at minimum:
- `dialog:openPdf`, `dialog:saveAs`
- `fs:readPdf`, `fs:writePdf`
- `recents:list`, `recents:add`, `recents:clear`
- `settings:get`, `settings:set`
- `bookmarks:list`, `bookmarks:upsert`, `bookmarks:delete` (per-file, keyed by file hash)
- `pdf:combine` (server-side merge via pdf-lib in main)
- `pdf:export` (Phase 2 channel but spec it now — request includes engine preference: `'auto' | 'pdf-lib' | 'chromium'`; response is `{engine, warnings[], outputBytes}`)
- `app:getVersion`, `app:quit`
- `app:setDefaultPdfHandler` (Decision 4 runtime toggle)
- `app:getDefaultPdfHandlerStatus` (read-only check)

For each channel: typed request shape, typed response shape, error mode (named error variants, NOT `any`), async semantics (one-shot vs stream).

Define the **shared TS types module** location: `src/ipc/contracts.ts` — David's file. Riley imports read-only.

### 3. `d:\Projects\PDF_Viewer_Editor\docs\data-models.md`
- **SQLite schema (DDL)**:
  - `schema_migrations (version INTEGER PRIMARY KEY, applied_at INTEGER)`
  - `recent_files (path TEXT PRIMARY KEY, last_opened_at INTEGER, file_hash TEXT, display_name TEXT)` + index on `last_opened_at`
  - `app_settings (key TEXT PRIMARY KEY, value TEXT)` — JSON-serialized values; include the `file_association.pdf.requested` key from Decision 4
  - `user_bookmarks (id INTEGER PK, file_hash TEXT, page_index INTEGER, title TEXT, created_at INTEGER, UNIQUE(file_hash, page_index, title))` + index on `file_hash`
- At least one example row per table
- **TS interfaces for in-memory document model**: `PDFDocumentModel`, `PageModel`, `AnnotationModel` (discriminated union by `subtype`), `EditOperation` (discriminated union for page reorder/insert/delete/rotate/annotation-add/annotation-edit/annotation-delete)
- **Annotation subtype mapping table** (from Decision 2): user-visible name → ISO 32000 subtype (`/Highlight`, `/Text`, `/FreeText`, `/Underline`, `/StrikeOut`, `/Ink`, `/Square`, `/Circle`, `/Line`) → phase → pdf-lib support level (native / requires manual dict authoring)
- **Page-operation command types** mapped to the `EditOperation` union

### 4. `d:\Projects\PDF_Viewer_Editor\docs\ui-spec.md`
Phase 1 screens:
- **Main viewer**: three-pane layout (sidebar | viewer | inspector). ASCII or Mermaid wireframe.
- **Sidebar**: tabs for Thumbnails | Bookmarks. Collapsible.
- **Toolbar**: Open, Save, Save As, Undo, Redo, annotation tools (Highlight, Sticky Note, Text Box; freehand/shapes disabled with "Phase 2" tooltip), page-ops menu (Insert, Delete, Rotate, Reorder), Combine
- **Modals**: combine-files picker, annotation properties (color, opacity, author), settings dialog (with the Decision 4 "Default PDF viewer" toggle)
- **Keyboard shortcuts table** (Ctrl+O, Ctrl+S, Ctrl+Shift+S, Ctrl+Z, Ctrl+Y, Del, Ctrl+W, Ctrl+Tab between tabs, etc.)
- **Drag-drop behavior**: file onto window opens; thumbnail drag reorders; drop external image (Phase 2 stub — disabled)
- **State per screen**: empty / loading / error / populated. Spell each one out.

### 5. `d:\Projects\PDF_Viewer_Editor\docs\conventions.md`
- TypeScript strict mode; no `any` without a code comment justifying why
- ESLint config target (`eslint:recommended` + `@typescript-eslint/recommended-type-checked` + `plugin:react/recommended` + `plugin:react-hooks/recommended`); Prettier with explicit config (printWidth, tabWidth, semi, etc.)
- File naming: kebab-case files, PascalCase components, `<name>-slice.ts` / `<name>-selectors.ts` for Redux
- Commit format: link to repo `CLAUDE.md` (do NOT duplicate)
- Import order (Node built-ins → external → internal alias → relative → styles)
- Error handling patterns: discriminated-union Result types vs throwing; user-facing error surface
- **Redux Toolkit slice conventions** (Decision 3): one concern per slice, selectors next to slice, `createAsyncThunk` for IPC, `meta.undoable: true` for undo-eligible actions, typed `useAppSelector` / `useAppDispatch`
- 200-line modularization rule (link to repo `CLAUDE.md`)

## Constraints (non-negotiable)

- **Permissive OSS only** (MIT / Apache-2.0 / BSD). Every library in ARCHITECTURE.md must have its license verified — name + version + license URL. AGPL / commercial SDKs are forbidden.
- **Electron security floor**:
  - `contextIsolation: true`
  - `nodeIntegration: false`
  - `sandbox: true` where compatible (pdf.js worker may force one renderer to relax this — document if so)
  - Strict CSP (give the exact string)
  - No remote module
  - IPC ONLY via the typed preload `contextBridge`
- **Walking-skeleton scope only** for what Wave 2 implements. Forms / OCR / scan / Office export / mail-merge / sign / image-import / Print-to-PDF are NOT in Wave 2. BUT architect with those phases in mind — extension points called out where sensible.

## What you must NOT do

- No code. No `src/` files. No `package.json`. No dependency installs. No `npm` commands. **Docs only.**
- Don't touch any file outside the five listed above.
- Don't write to `.learnings/learnings.jsonl` directly — return your post-flight log entry as a JSON object in your final response so Marcus can append serially (Hard-Won Playbook §1).

## Output budget
Thorough but pragmatic. These docs are read by 3 implementation agents and 2 reviewers in Wave 2 and Wave 3. Aim for "complete enough that David / Ravi / Riley(impl) start implementing without further questions" — not "exhaustive treatise."

## When you finish
Return:
1. A summary line per delivered doc with absolute path and line count
2. Any open questions for Marcus
3. A JSON object representing your `.learnings/learnings.jsonl` entry (Marcus appends serially)
